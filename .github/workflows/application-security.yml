# Everything visible in the Actions tab is named after what it does, not after the
# tool that does it. Product names live in the secrets and in the documentation.

name: Application security

# Pull requests only ever run the component check, and only when a dependency
# actually changed. Source code analysis runs on main and on demand, never on a
# pull request: this repository carries a batch of automated remediation PRs, and
# a full source scan on each of them would be slow, noisy and pointless.
on:
  push:
    branches: [main]
  pull_request:
    paths:
      - pom.xml
  workflow_dispatch:

permissions:
  contents: read
  security-events: write
  pull-requests: write

jobs:

  static-analysis:
    name: Static code analysis
    runs-on: ubuntu-latest
    # Never on a pull request. See the trigger comment above.
    if: github.event_name != 'pull_request'
    steps:
      - name: Check out the code
        uses: actions/checkout@v5

      - name: Set up the build environment
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin
          cache: maven

      # One step: package the source, submit it, wait for the result, evaluate the
      # policy and publish the findings into the Security tab. The analysis engine
      # is the same one a developer can run locally before pushing.
      - name: Analyse the source code and publish findings
        uses: fortify/github-action@v3
        env:
          # Public regional endpoint, not a secret.
          FOD_URL: https://api.emea.fortify.com
          FOD_TENANT: ${{ secrets.FOD_TENANT }}
          FOD_USER: ${{ secrets.FOD_USER }}
          FOD_PASSWORD: ${{ secrets.FOD_PAT }}
          FOD_RELEASE: ${{ vars.FOD_RELEASE }}
          DO_SETUP: "false"
          # Open source is analysed in its own job below, by a dedicated engine.
          DO_SCA_SCAN: "false"
          DO_AVIATOR_AUDIT: "true"
          PACKAGE_EXTRA_OPTS: -bt mvn
          DO_CHECK_POLICY: "true"

  open-source-analysis:
    name: Open source analysis
    runs-on: ubuntu-latest
    steps:
      - name: Check out the code
        uses: actions/checkout@v5

      - name: Set up the build environment
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: temurin
          cache: maven

      # Indexing writes the exact Maven relationships alongside the built artefact.
      # Scanning both is what produces a real dependency tree rather than a graph
      # reconstructed from a public registry.
      - name: Build and resolve the dependency tree
        run: |
          mvn -B -ntp -DskipTests package
          mvn -B -ntp com.sonatype.clm:clm-maven-plugin:index

      - name: Evaluate components against policy
        id: evaluate
        uses: sonatype/actions/evaluate@v1
        with:
          iq-server-url: ${{ secrets.SONATYPE_IQ_URL }}
          username: ${{ secrets.SONATYPE_IQ_USER }}
          password: ${{ secrets.SONATYPE_IQ_PASSWORD }}
          application-id: direct-to-patient-portal
          stage: develop
          scan-targets: target/sonatype-clm/module.xml target/iwa.jar
          sarif-file: components.sarif
          # Uploaded further down, after aggregation and link scrubbing.
          upload-sarif-file: false

      # One result per policy violation would bury the code findings. This collapses
      # them to one alert per component, carrying the full vulnerability list.
      #
      # --public-links is mandatory: this repository is public and the deep links
      # would otherwise embed the internal hostname. The script fails the job if any
      # trace of that hostname survives into the uploaded file.
      #
      # --security-only drops components whose violations are licence or quality
      # only. Licence policy and waivers are handled in the platform, not here.
      - name: Summarise findings per component
        if: ${{ always() && hashFiles('components.sarif') != '' }}
        run: |
          python3 .github/scripts/aggregate-sonatype-sarif.py \
            components.sarif components-summarised.sarif --public-links --security-only

      - name: Publish findings to the Security tab
        if: ${{ always() && hashFiles('components-summarised.sarif') != '' }}
        uses: github/codeql-action/upload-sarif@v3
        with:
          sarif_file: components-summarised.sarif
          category: open-source-analysis

      # ---------------------------------------------------------------------
      # Bring the component findings into the same release as the code findings,
      # so that one gate decides on both. The bill of materials is the transport.
      # ---------------------------------------------------------------------

      - name: Export the software bill of materials
        id: sbom
        if: ${{ success() || steps.evaluate.outcome == 'failure' }}
        uses: sonatype/actions/fetch-sbom@v1
        with:
          iq-server-url: ${{ secrets.SONATYPE_IQ_URL }}
          username: ${{ secrets.SONATYPE_IQ_USER }}
          password: ${{ secrets.SONATYPE_IQ_PASSWORD }}
          application-id: direct-to-patient-portal
          scan-id: ${{ steps.evaluate.outputs.scan-id }}
          sbom-standard: cycloneDx
          sbom-format: json
          sbom-version: '1.6'
          artifact-name: bill-of-materials

      # This action publishes the exported file as a build artifact, and on a public
      # repository anyone can download it. Fail loudly if the internal hostname ever
      # turns up inside it, rather than discovering that later.
      - name: Check the exported file for internal hostnames
        if: ${{ steps.sbom.outcome == 'success' }}
        env:
          IQ_URL: ${{ secrets.SONATYPE_IQ_URL }}
        run: |
          HOST=$(printf '%s' "$IQ_URL" | sed -E 's#^https?://##; s#/.*$##')
          if grep -qiF "$HOST" "${{ steps.sbom.outputs.sbom-file-path }}"; then
            echo "::error::The exported bill of materials contains the internal hostname. Not proceeding."
            exit 1
          fi
          echo "Clean: no internal hostname in the exported file."

      # Each finding gets a line pointing back to where the decision actually lives.
      # It arrives as plain text, not as a link — the platform escapes any markup.
      - name: Add a pointer back to the system of record
        if: ${{ steps.sbom.outcome == 'success' }}
        env:
          IQ_URL: ${{ secrets.SONATYPE_IQ_URL }}
        run: |
          jq --arg iq "$IQ_URL" '.vulnerabilities |= map(
            .recommendation = "Sonatype Lifecycle is the system of record for this open source finding. "
              + "Full vulnerability research, affected versions, policy decision and remediation guidance: "
              + $iq + "/ui/links/vln/" + .id)' \
            "${{ steps.sbom.outputs.sbom-file-path }}" > bom-linked.json
          echo "Vulnerabilities carrying a pointer: $(jq '[.vulnerabilities[] | select(.recommendation)] | length' bom-linked.json)"

      - name: Set up the release tooling
        if: ${{ steps.sbom.outcome == 'success' }}
        uses: fortify/github-action/setup@v3
        with:
          export-path: true
          fcli: latest

      # Import can take several minutes on a large bill of materials.
      - name: Attach the component findings to the release
        if: ${{ steps.sbom.outcome == 'success' }}
        env:
          FOD_URL: https://api.emea.fortify.com
          FOD_TENANT: ${{ secrets.FOD_TENANT }}
          FOD_USER: ${{ secrets.FOD_USER }}
          FOD_PASSWORD: ${{ secrets.FOD_PAT }}
          FOD_RELEASE: ${{ vars.FOD_RELEASE }}
        run: |
          fcli fod session login --url "$FOD_URL" --tenant "$FOD_TENANT" \
            --user "$FOD_USER" --password "$FOD_PASSWORD" --fod-session sbom-import
          fcli fod oss-scan import -f bom-linked.json --rel "$FOD_RELEASE" --fod-session sbom-import
          fcli fod session logout --fod-session sbom-import

  release-gate:
    name: Release gate
    runs-on: ubuntu-latest
    needs: [static-analysis, open-source-analysis]
    if: always()
    steps:
      - name: Decide whether this release may proceed
        run: |
          CODE="${{ needs.static-analysis.result }}"
          DEPS="${{ needs.open-source-analysis.result }}"
          echo "Source code analysis ..... $CODE"
          echo "Open source analysis ..... $DEPS"
          # "skipped" is a valid outcome: on a pull request only the component
          # check runs, and that check alone decides the gate.
          for r in "$CODE" "$DEPS"; do
            case "$r" in
              success|skipped) ;;
              *) echo "::error::A security check did not pass. This build is blocked."; exit 1 ;;
            esac
          done
          echo "Every check that ran passed. This build may proceed."
